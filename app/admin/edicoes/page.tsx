import {redirect} from "next/navigation";import {requireChatGPTUser} from "../../chatgpt-auth";import AdminEditor from "../AdminEditor";import "../admin.css";export const dynamic="force-dynamic";export default async function EditionsAdmin(){const user=await requireChatGPTUser("/admin/edicoes");if(user.email.toLowerCase()!=="wallissonghost@gmail.com")redirect("/");return <AdminEditor userName={user.displayName}/>}
